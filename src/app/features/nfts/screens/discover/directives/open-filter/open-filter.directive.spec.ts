import { OpenFilterDirective } from './open-filter.directive';

describe('OpenFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenFilterDirective();
    expect(directive).toBeTruthy();
  });
});
